export type MusicSearchData = {
  title: string
  artist: string
  coverURL: string
  releasedDate: string
}

export type MusicData = {
  musicId: number
  title: string
  artist: string
  releasedDate: string
  videoId: string
  coverURL: string
}
